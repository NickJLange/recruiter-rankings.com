# Example environment variables for local dev and Render deployment
# Copy to .env and adjust values as needed (do not commit secrets)

# Common
RAILS_MAX_THREADS=5
RAILS_LOG_LEVEL=info
WEB_CONCURRENCY=1
PORT=3000

# Production (Render) — set these in Render Dashboard or via render.yaml
# DATABASE_URL is provided automatically by Render if you add a Managed PostgreSQL resource
# SECRET_KEY_BASE should be set to a value from `rails secret`
# DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME
# SECRET_KEY_BASE=REPLACE_ME

# App configuration (set in env, not in code)
# Admin basic auth (development defaults are mod/mod if unset)
# DEMO_MOD_USER=mod
# DEMO_MOD_PASSWORD=mod
# LinkedIn fetch tuning
# LINKEDIN_FETCH_TIMEOUT=5
# LINKEDIN_FETCH_UA=RecruiterRankingsBot/0.1
# Email HMAC pepper (generate a random value for production)
# SUBMISSION_EMAIL_HMAC_PEPPER=REPLACE_WITH_RANDOM
# Public directory thresholds and pagination
# PUBLIC_MIN_REVIEWS=5
# PUBLIC_PER_PAGE=10
# PUBLIC_MAX_PER_PAGE=50
# Demo flags
# DEMO_AUTO_APPROVE=false
# SUBMISSION_COPY_OVERALL_TO_DIMENSIONS=true
# Canonical URL for sitemap and meta
# CANONICAL_URL=https://recruiter-rankings.com

# Development overrides (optional)
# RAILS_ENV=development
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/web_development

